<nav class="navbar" id="nav">
    <div class="container">
        <div id="navMenu" class="navbar-menu">
            <div class="navbar-start ">
                <div class="has-text-white has-text-weight-bold navbar-item">
                    Martin Bissiger
                </div>
                <div class="navbar-item is-hidden-tablet">
                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                        <p class="control">
                            <a class="button" href="/logout">
                              <span class="icon">
                                <i class="fas fa-sign-in-alt"></i>
                              </span>
                                <span>Logout</span>
                            </a>
                        </p>
                    {% else %}
                        <p class="control">
                            <a class="button is-transparent" href="/login">
                              <span class="icon">
                                <i class="fas fa-sign-in-alt"></i>
                              </span>
                                <span>Login</span>
                            </a>
                        </p>
                    {% endif %}
                </div>
            </div>
            <div class="navbar-end">
                <a href="/" class="navbar-item">
                    Home
                </a>
                {# prototyp
                    {% for section in sections %}
                        <a href="#{{ section.name|lower }}" class="navbar-item">
                            {{ section.name }}
                        </a>
                    {% endfor %}
                #}
                <a href="#about" class="navbar-item">
                    About
                </a>
                <a href="#hobby" class="navbar-item">
                    Hobbies
                </a>
                <a href="#work" class="navbar-item">
                    Work
                </a>
                <a href="#contact" class="navbar-item">
                    Contact
                </a>
                <div class="navbar-item is-hidden-mobile">
                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                        <p class="control">
                            <a class="button" href="/logout">
                              <span class="icon">
                                <i class="fas fa-sign-in-alt"></i>
                              </span>
                                <span>Logout</span>
                            </a>
                        </p>
                    {% else %}
                        <p class="control">
                            <a class="button is-transparent" href="/login">
                              <span class="icon">
                                <i class="fas fa-sign-in-alt"></i>
                              </span>
                                <span>Login</span>
                            </a>
                        </p>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</nav>

