<nav class="navbar" id="nav">
    <div class="container">
        <div id="navMenu" class="navbar-menu">
            <div class="navbar-start ">
                <div class="has-text-white has-text-weight-bold navbar-item">
                    <a href="/">
                    {% if is_granted('ROLE_DEV') %}
                        Martin Bissiger
                    {% elseif is_granted('ROLE_USER') %}
                            Hi {{ app.user.username }}
                        {% else %}
                           Martin Bissiger
                    {% endif %}
                    </a>
                </div>
                <div class="navbar-item is-hidden-tablet">
                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                        <p class="control">
                            <a class="button" href="/logout">
                              <span class="icon">
                                <i class="fas fa-sign-in-alt"></i>
                              </span>
                                <span>Logout</span>
                            </a>
                        </p>
                    {% else %}
                        <p class="control">
                            <a class="button is-transparent" href="/login">
                              <span class="icon">
                                <i class="fas fa-sign-in-alt"></i>
                              </span>
                                <span>Login</span>
                            </a>
                        </p>
                    {% endif %}
                </div>
            </div>
            {% set route = app.request.attributes.get('_route') != 'homepage' ? path('homepage') : '' %}
            {% if route is empty %}
                <a class="readMe is-flex is-align-items-center" onclick="window.openModal('readme-modal')"> {# modal id in onclick #}
                    <i class="fas fa-book-reader bouncing {% if is_granted('ROLE_DEV') %}no-animation{% endif %}"></i><span class="ml-3 has-text-weight-bold has-text-white is-size-4">Docs</span>
                </a>
            {% endif %}
            <div class="navbar-end">

                <a href="{{ route }}#about" class="navbar-item">
                    About
                </a>
                <a href="{{ route }}#hobby" class="navbar-item">
                    Hobbies
                </a>
                <a href="{{ route }}#project" class="navbar-item">
                    Work
                </a>
                <a href="{{ route }}#contact" class="navbar-item">
                    Contact
                </a>
                <div class="navbar-item is-hidden-mobile">
                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                        <p class="control">
                            <a class="button" href="/logout">
                              <span class="icon">
                                <i class="fas fa-sign-in-alt"></i>
                              </span>
                                <span>Logout</span>
                            </a>
                        </p>
                    {% else %}
                        <p class="control">
                            <a class="button is-transparent" href="/login">
                              <span class="icon">
                                <i class="fas fa-sign-in-alt"></i>
                              </span>
                                <span>Login</span>
                            </a>
                        </p>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</nav>

